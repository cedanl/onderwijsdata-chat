apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  - helmrepo.yaml
  - network-policy.yaml
configMapGenerator:
  - name: base-config
    files:
      - values.yaml=values-base.yaml
configurations:
  - kustomizeconfig.yaml
