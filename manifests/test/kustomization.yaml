apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - helmrelease.yaml
  - helmrepo.yaml
  - middleware.yaml
  - secret.yaml

configMapGenerator:
  - name: env-config
    files:
      - values.yaml=values.yaml

configurations:
  - ../base/kustomizeconfig.yaml
